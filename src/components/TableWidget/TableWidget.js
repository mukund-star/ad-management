import React from 'react';
import PropTypes from 'prop-types';
import { TableWidgetWrapper } from './TableWidget.styled';

const TableWidget = () => (
 <TableWidgetWrapper>
    TableWidget Component
 </TableWidgetWrapper>
);

TableWidget.propTypes = {};

TableWidget.defaultProps = {};

export default TableWidget;
