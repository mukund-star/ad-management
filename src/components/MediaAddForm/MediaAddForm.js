import React from 'react';
import PropTypes from 'prop-types';
import { MediaAddFormWrapper } from './MediaAddForm.styled';

const MediaAddForm = () => (
 <MediaAddFormWrapper>
    MediaAddForm Component
 </MediaAddFormWrapper>
);

MediaAddForm.propTypes = {};

MediaAddForm.defaultProps = {};

export default MediaAddForm;
